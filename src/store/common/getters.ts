import { GetterTree } from 'vuex';
import { ICommonState } from '@/store/common';
import { IAppState } from '@/store/models';

const getters: GetterTree<ICommonState, IAppState> = {
};

export default getters;
